## Packages
(none needed)

## Notes
Uses existing shadcn/ui components already in repo (button, card, dialog, input, scroll-area, badge, tabs, separator, skeleton, tooltip, toaster).
Uses framer-motion (already installed) for page transitions and staggered reveals.
Adds data-testid to key UI elements and all interactive controls.
SEO titles/descriptions set per route via a small hook that updates document.title and meta[name="description"].
